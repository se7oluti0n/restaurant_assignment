

  <% if params[:search] and not @food_items.present? %>
    <h4 style="margin-top:40px;margin-bottom:40px"> Sorry, there is no post containing "<%= params[:search] %>"</h4>
  <% elsif @food_items  %>
      <%= render @food_items %>
  <%else %>
  <div class="full">
	<div class="btn-group centered" role="group" aria-label="Basic example">
		<%= link_to "Menu", menu_path, class: "btn btn-secondary btn-lg" %> 
		<%= link_to "Contact", contact_path, class: "btn btn-secondary btn-lg" %>
	</div>
	</div>
  <% end %> 
